apiVersion: v1
kind: Service
metadata:
  name: nginx-ip
  namespace: kube-system
  annotations:
    external-dns.alpha.kubernetes.io/hostname: nginx.<path:stringreplacesecret#domain>
spec:
  type: ExternalName
  externalName: 128.140.15.44