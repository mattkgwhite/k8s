---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: jenkins
  namespace: argocd
spec:
  project: default
  source:
    repoURL: "https://github.com/mattkgwhite/homeops.git"
    path: manifests/workloads/ci/jenkins
    targetRevision: main
  destination:
    server: "https://kubernetes.default.svc"
    namespace: jenkins
  syncPolicy:
    automated:
      prune: true
    syncOptions:
      - CreateNamespace=true