---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: vault-cert
  namespace: vault
spec:
  commonName: vault.robotwizard.co.uk
  secretName: vault-cert
  dnsNames:
    - vault.robotwizard.co.uk
  issuerRef:
    name: acme-prod
    kind: ClusterIssuer
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: vault
  namespace: vault
  annotations:
    cert-manager.io/cluster-issuer: acme-prod
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - vault.robotwizard.co.uk
    secretName: vault-cert
  rules:
  - host: vault.robotwizard.co.uk
    http:
      paths:
      - backend:
          service:
            name: vault
            port:
              number: 8200
        path: /
        pathType: Prefix