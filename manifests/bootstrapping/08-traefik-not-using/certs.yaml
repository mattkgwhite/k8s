apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: traefik-cert
spec:
  commonName: traefik.hacksm.net
  secretName: traefik-cert
  dnsNames:
  - traefik.hacksm.net
  issuerRef:
    name: acme-prod
    kind: ClusterIssuer