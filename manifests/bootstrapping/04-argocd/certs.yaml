---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: argo-robotwizard-cert
  namespace: kube-system
spec:
  commonName: argo.robotwizard.co.uk
  secretName: argo-robotwizard-cert
  dnsNames:
    - argo.robotwizard.co.uk
  issuerRef:
    name: acme-prod
    kind: ClusterIssuer